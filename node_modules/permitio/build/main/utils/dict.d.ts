export type Dict = Record<string, any>;
export declare function isDict(val: any): val is Dict;
/**
 * JS Equivalent to python dict(zip())
 *
 * generates a dict-like object (record) from two same-size lists
 * if lists are not same-size, returns undefined.
 * @param keys
 * @param values
 */
export declare function dictZip(keys: string[], values: string[]): Record<string, string> | undefined;
