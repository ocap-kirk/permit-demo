import express from 'express';
import { MappedEndpoint } from '../types';
export declare const KEY_DELIMITER = ":";
export declare const PATH_DELIMITER = "/";
export declare function mapExpressAppEndpoints(app: express.Express): MappedEndpoint[];
