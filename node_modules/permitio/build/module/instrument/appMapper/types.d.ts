export interface MappedEndpoint {
    path: string;
    methods: string[];
    middleware: any | undefined[];
    namedMethods: Record<string, string>;
    methodToCallable: Record<string, any>;
    regEx: RegExp;
    keys: string[];
    prefix?: string;
}
export declare enum RoutesStyle {
    REST = 1,
    RESTHOLE = 2
}
export declare type EndpointGroup = (MappedEndpoint | EndpointTree)[];
export declare type EndpointTree = Record<string, EndpointGroup>;
