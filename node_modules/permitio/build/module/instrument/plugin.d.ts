import { Logger } from 'winston';
import { AppManager } from './appManager';
/**
 * Plugin (hook) into frameworks (Express) and track created apps
 */
export declare function hook(appManager: AppManager, logger: Logger): void;
