import { Logger } from 'winston';
import { IPermitConfig } from '../config';
import { ResourceConfig } from '../resources/interfaces';
import { ResourceReporter } from '../resources/reporter';
import { MappedEndpoint } from './appMapper/types';
export declare class AppManager {
    private config;
    private resourceReporter;
    private logger;
    resources: Record<string, any>;
    constructor(config: IPermitConfig, resourceReporter: ResourceReporter, logger: Logger);
    report(name: string, resources: ResourceConfig[], endpoints: MappedEndpoint[]): void;
    manage(name: string, app: any): void;
}
