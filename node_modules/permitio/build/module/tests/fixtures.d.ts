import { ExecutionContext } from 'ava';
import pino from 'pino';
import { IPermitClient, PermitApiError } from '../index';
export type TestContext = {
    permit: IPermitClient;
    logger: pino.Logger;
};
export declare const printBreak: () => void;
export declare function handleApiError(error: PermitApiError<any>, message: string, t: ExecutionContext<TestContext>): void;
export declare const provideTestExecutionContext: (t: ExecutionContext<TestContext>) => void;
